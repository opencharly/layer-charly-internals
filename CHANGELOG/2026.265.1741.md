# 2026.265.1741 — docs(git-workflow): sweep the residual per-repo-dispatcher POISON remedies

## Summary

Follow-up sweep on `layer-charly-internals`'s `git-workflow` skill: the **POISON-state remedies** section still described the concurrency dedupe as **per-repo opt-in**, which is obsolete since the org-ruleset cutover.

Claim-keyed sweep:

- **"Add the per-repo concurrency dedupe to that repo's dispatcher"** → the dedupe lives in the **ONE org required workflow** (`org-wide-pr-validator-required.yml`), so the class is fixed org-wide; the whole per-repo opt-in remedy (and its `417 of 420 dispatchers did NOT` measurement) is replaced.
- **"Full mechanics, the dedupe YAML, and which repos carry it"** → "Full mechanics and the dedupe YAML" (there are no per-repo dispatchers to enumerate).
- **"the dispatchers declare `on: pull_request: …`"** → "the org required workflow declares …".
- Dropped the "Add the dedupe to the org template" remedy (it IS the org workflow now).

Generated projection regenerated separately (marketplace) once this source lands and tags.

## How tested

Documentation-only change class; no runtime bed.

```console
$ grep -nE "per-repo dispatcher|dispatcher stub|which repos carry it|417 —|per-repo OPT-IN|Add the dedupe to the org template|the dispatchers declare|14 today|branch-protection\.sh" layer-charly-internals/charly.yml
$ echo $?
1
```

(The sweep prints nothing — no stale per-repo-dispatcher claim survives.)

## Rulebook compliance

- **R0 skills:** `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1 RCA:** the POISON remedy section was an unreconciled leftover from before the org-ruleset cutover; swept claim-keyed.
- **R2 complete:** every variant fixed here.
- **R3 no duplication:** the skill names the single org workflow.
- **R4 no workaround:** the org-wide fix is stated as the real one, not a per-repo opt-in.
- **R5:** the claim-keyed sweep is executed above.
- **R6 git safety:** branch + PR; no force-push.
- **R7 gate:** the sweep output pasted above.
- **Change class:** documentation-only → `documentation reviewed`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*