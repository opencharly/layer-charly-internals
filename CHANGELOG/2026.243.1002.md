# 2026.243.1002 — docs(skills): drop the non-existent cue_kind_vm.go reference (spec CUE registry phrasing)

## Summary

M3 residual fix: the vm-spec + libvirt-renderer skill entities referenced `cue_kind_vm.go`, which does not exist in spec/sdk/charly/plugin-vm (verified against the pinned modules + charly main). The per-kind CUE registration for the VM schemas lives in the spec CUE registry; the skills now state that stable spec-source without a concrete (non-existent) filename, per the skills-skill's alias rule.

Changes (`charly.yml`, 3 Source-table spots in the vm-deploy-target + vm-spec entities): `cue_kind_vm.go` → `spec/schema/vm.cue (registered per-kind via the spec CUE registry)`.

## How tested

- grep `cue_kind_vm` in charly.yml = 0 after; YAML parses.
- Verified the file does not exist anywhere current: spec@v0.2026242.1252-…, sdk@v0.2026242.1217, plugin-vm HEAD, charly main (audit source-map sweep evidence).

## Rulebook compliance

- R5: stale identifier removed; replacement names the real registry.
- R3: no duplication; phrasing per the skills-skill alias guidance.

## Change classification

- Change class: Documentation-only (candy skill entities).
- Verification gate: documentation reviewed (grep + YAML parse).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*