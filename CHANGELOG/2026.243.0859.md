# 2026.243.0859 — fix(skills): correct skill-authoring instructions to the owning-repo + regeneration reality

## Summary

Correct the `charly-internals:skills` authoring instructions to match the post-cutover reality (audit finding §12.1, DIVERGES).

The skill's "How to Update" told authors to edit `candy/<candy>/charly.yml` in the charly repo — but the candy de-submodule cutover emptied charly/candy/ (the charly repo now owns NO `skill:` entities); each family's `skill:` entities live in its standalone owning candy repo (internals → layer-charly-internals + layer-charly-internals-extra, build → layer-charly-build, check → layer-charly-check, kubernetes → layer-charly-kubernetes, image → layer-charly-image, core → pod-charly-core, automation → pod-charly-automation, local → layer-charly-local, pod → layer-charly-pod, hooks/verbs → pod-charly-hooks), pinned by the marketplace refs list (`candy/charly-marketplace/charly.yml`), with regeneration via `charly -C <marketplace> marketplace generate --root <marketplace> --out <marketplace>` under the deploy drift gate.

Changes:
- `charly.yml` (`skills` skill entity): step 1 of "How to Update" now points at the owning-repo map + regeneration command + drift-gate contract, cross-referencing the new `/charly-internals:marketplace` skill.

## How tested

- YAML parses (`yaml.safe_load` OK); old text gone (grep count 0), new text present (count 1).
- The owning-repo map + regeneration mechanics match the live audit evidence (plan/evidence/ph1/marketplace-mechanics.md; regeneration executed successfully during the audit; opencharly/layer-charly-internals#4 introduces the referenced `/charly-internals:marketplace` skill).
- No generated SKILL.md edited; source-only change.

## Rulebook compliance

- R1: the divergence (doc vs reality) was RCA'd against the owning-repo map extracted from the refs list + repo layout; not assumed.
- R4: no workaround; text now describes the real mechanism.
- R5: the stale instruction is removed; the replacement is the current mechanism.

## Change classification

- Change class: Documentation-only (candy skill entity).
- Verification gate: documentation reviewed (YAML parse + grep evidence; no beds needed).