# 2026.250.1956 — feat(skill): agents — every delegated worker MUST read the relevant skill before first tool call

## Summary

Adds a hard R1-derived precondition to the agents skill: no sub-agent/dynamic-workflow child may start working without reading the SKILL.md(s) its task triggers, and the parent brief must name them. Documented with the measured failures (a worker ran check live instead of check run for an R10 proof — 26 skipped mutating steps + 43 downstream failures; a worker pushed four un-gated heads).

## How tested

Source-level docs change in the owning candy skill: entity; the marketplace corpus regeneration + drift gate land at the refs-bump PR (same wave).

## Rulebook compliance

- R1: the directive encodes the measured incidents as a hard rule.
- R6: PR-only; no force-push; no direct main push.
- R3: single canonical source; no corpus hand-edits.

## Change classification

Class: documentation/skill-source. Gate: repo validate + marketplace regeneration. Attribution: **documentation reviewed**.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*