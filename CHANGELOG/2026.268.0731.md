# 2026.268.0731 — docs(skill): retire /verify-beds; use the native check-roster

## Summary

Retire the `/verify-beds` references in this family's skill source, repointing them at the native plugin engine `kind:check-roster` (`charly check run <roster>`), which now owns roster runs (bounded `lanes`, exclusive-token serialization, host-local refusal, honest exit aggregation).

Files: the skill entity's R10/roster/change-class/exit-code prose (`charly.yml`), plus a schema `version:` migration (`charly migrate`, version line only).

## How tested

```
$ charly box validate -C .
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
$ grep -c 'verify-beds' charly.yml
0
```

## Rulebook compliance

- R0: `/charly-check:check`, `/charly-internals:skills`.
- R3 no duplication: the roster engine is the ONE home for the roster classifier; the skill points there.
- R5 hard cutover + grep: `grep -c 'verify-beds' charly.yml` → 0.
- R1/R6/R10: docs-only skill prose; no runtime path; no destructive action.

## Classification

- **Change class:** documentation/skill-source (generated corpus regenerated from it).
- **Verification gate:** `charly box validate` (pasted).
- **Attribution tier:** `documentation reviewed`.

---

*Assisted-by: opencode ollama-cloud deepseek-v4.1-flash (documentation reviewed)*