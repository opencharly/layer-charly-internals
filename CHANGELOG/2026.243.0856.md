# 2026.243.0856 — feat(skills): add the marketplace/skill-generation skill (charly-internals:marketplace)

## Summary

Add a new `charly-internals:marketplace` skill documenting how the skill/marketplace corpus is actually generated, refreshed, and consumed — operator-requested and grounded in a live audit of the pipeline.

The audit (Phase 0/1 of the skills-vs-reality effort) found that no skill documents the pipeline end-to-end: the charly repo owns NO `skill:` entities post cutover, each family lives in its standalone owning candy repo pinned by `candy/charly-marketplace/charly.yml` (the ONE generation input), and the corpus is regenerated with `charly marketplace generate` (verb resolves only via a `discover:` charly.yml; schema-support prerequisite) under the deploy workflow's fail-closed drift gate, with a daily refresh opening `chore: refresh the refs list` PRs, and per-harness caches (pi `.pi/pi-claude-marketplace`, Claude Code `charly-plugins`, ...).

Changes:
- `charly.yml`: new `marketplace-skill:` entity (name `marketplace`, family `internals`, owner `charly-internals`) with description/triggers/content covering the ownership split table, regeneration command + schema prerequisite, drift gate (incl. the untracked-files catch), the daily refresh, and per-harness vendoring.

## How tested

- YAML parses (`yaml.safe_load` OK); entity count verified (`name: marketplace` present once); file remains the repo's generated-candy source (no SKILL.md hand-edited anywhere).
- Content is present-tense and derived from verified mechanics: deploy.yml/refresh.yml read in full, the refs list read in full, regeneration executed live (see opencharly/marketplace#259 for the regenerated-projection proof), schema-gate behavior reproduced.
- Projection check: the corpus regenerates this skill into `internals/skills/marketplace/SKILL.md` on the next marketplace regeneration (refs/refresh cycle) — no committed projection in this repo.

## Rulebook compliance

- R3: no duplication — no existing skill covers the pipeline; the owning-repo map is stated once here and cross-referenced from `/charly-internals:skills`.
- R4: no workarounds — documentation only, no code patched.
- Source-only: the change is to the candy `skill:` entity; nothing generated was hand-edited.

## Change classification

- Change class: Documentation-only (candy skill entity).
- Verification gate: documentation reviewed (YAML parse + content review; no beds needed).