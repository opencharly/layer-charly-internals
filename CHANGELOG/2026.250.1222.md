# 2026.250.1222 — docs(egress): document the crabbox-yaml egress kind

## Summary

Docs-only update bringing the skill corpus in line with the merged **`config:` plan-step verb** wave (spec#117-120, sdk#237, plugin-box#11, plugin-egress#3 — charly config-file generation):

- The owning skill entity text now reflects the shipped surface. (The generated SKILL.md copy regenerates via `charly marketplace generate` once the owning repo's tag is bumped in the marketplace refs list — the corpus is generated, never hand-edited.)

## How tested

Diff of this PR (docs-only, charly.yml skill entity content):
- the changed rows/text are the ones pasted in the diff — no pins, no scripts, no code touched.
- The generated corpus update lands via the marketplace refs bump + `charly marketplace generate` (next PR in the wave), where the drift gate re-verifies the regenerated SKILL.md matches this source.

## Rulebook compliance

- R3: the corpus is generated from THIS source entity — no hand-edited generated copies.
- R7: docs-only change class verified by the diff; the regenerated corpus is proven by the marketplace drift gate at the next refs bump.

## Change classification

- **Class:** documentation (skill entity source). **Verification gate:** diff review + the marketplace regeneration drift gate (next PR). **Attribution:** documentation reviewed (docs-only change class; no pins/scripts changed).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*