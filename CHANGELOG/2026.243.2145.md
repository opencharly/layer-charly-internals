# 2026.243.2145 — docs(skills): tighten the 19 internals skill descriptions to trigger-style

## Summary

Tighten the `description:` frontmatter of all 19 charly-internals skill entities to trigger-style index lines ("Use when X"). The descriptions are the skill index injected into every harness system prompt (progressive disclosure: only name + description + location are always in context; the full SKILL.md body loads on demand). Shorter, trigger-style descriptions shrink the index without touching skill names, bodies, or discoverability — **zero capability reduction**: all 19 skills remain discoverable and loadable.

Total description bytes for the internals family: ~8,000 → 4,580 (~45% reduction).

## How tested

- `python3` YAML parse of the edited `charly.yml`: OK — all 19 skill entities parse, descriptions verified.
- `git diff` review: only `description:` blocks changed (41 insertions / 115 deletions); no skill name, body, trigger, or other field touched.
- The change is metadata-only prose; no behavior, schema, or build surface affected.

## Rulebook compliance

- **R3** — no duplication: each description is a single canonical index line.
- **R5** — no stale references: the new descriptions keep the same routing signal (same triggers, same "use before X" contract).
- No pins, no scripts, no schema changes, no direct push to main.

## Change classification

- **Change class:** docs-only (skill-description metadata prose; zero behavior change)
- **Verification gate:** YAML parse + diff review
- **Attribution tier:** `documentation reviewed`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*