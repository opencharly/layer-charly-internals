# 2026.268.0756 — fix: revert the schema version bump (repo CI pins charly v2026.241.1121)

## Summary

Revert the accidental `version:` bump in the preceding skill-prose change. The prose edit (retiring `/verify-beds` references in favor of the native `kind:check-roster` engine) does **not** require a schema migration, but the merged change carried `version: 2026.261.1747` from a `charly migrate` run — and this repo's CI (`.github/workflows/deploy.yml`) pins an older `charly` whose max schema is `2026.240.1943`. The bump therefore failed the repo's own `build` gate.

Files: `charly.yml` — restore `version: 2026.240.1943`; the prose changes are retained.

## Change class

**Config** — this repo's (`opencharly/layer-charly-internals`) `charly.yml` box-manifest schema `version:` line. (Not documentation: a config file changed.)

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this class (config/manifest):** this repo's own `build` CI check — `.github/workflows/deploy.yml` clones the pinned charly (`v2026.241.1121`) and runs `charly box validate` on this manifest.
- **Eval beds run:** `N/A` — no runtime/deploy entity changed.
- **Fresh rebuild:** `N/A` — no binary.
- **Did the CHANGED code path execute live?** Yes — this repo's CI `build` check (`validate at the pinned charly`) ran and is now GREEN (previously failed on the schema bump).
- **Concurrency:** `N/A`.

*1. THIS PR's `build` gate is GREEN (verbatim):*

```
$ gh pr checks -R opencharly/layer-charly-internals 21
build               pass    59s
validate / validate fail    ...   (the fresh-validator BLOCK on THIS body, which this rewrite addresses; the repo `build` GATE itself is green)
```

(Before this revert the same `build` job failed: `config schema 2026.261.1747 is newer than this charly supports (max 2026.240.1943)`.)

*2. The prose retirement is retained (verbatim):*

```
$ grep -c 'verify-beds' charly.yml
1     (the one intentional "the former /verify-beds ... is DELETED" note)
$ grep -m1 '^version:' charly.yml
version: 2026.240.1943
```

## Attribution tier

`fully tested and validated` — the config-class gate (this repo's `build` CI check: `charly box validate` at the pinned charly) ran and passed on this head.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:skills`, `/charly-build:validate`.
- **R1 RCA + zero warnings:** RCA — the bump was unnecessary for a prose edit and broke this repo's own CI gate; root fix is to restore the schema version.
- **R2 no "out of scope":** the regression is fixed here, not deferred.
- **R3 no duplication:** no behavior added.
- **R4 no workaround:** the real fix (restore the schema version), not a CI suppression.
- **R4a fix the product before the prose:** the fix restores the config the CI validates against.
- **R5 hard cutover + grep:** the prose retirement is retained (the single remaining `verify-beds` mention is the intentional "former … DELETED" note).
- **R6 git safety:** `git status` checked; no destructive action.
- **R7 runtime gate:** `N/A` — no runtime path (config version line).
- **R8 artifact invariants:** the manifest validates at the pinned charly (the emitted config).
- **R9 binary == source + deps:** `N/A` — no binary.
- **R10 disposable + coverage:** `N/A` — no `disposable: true` target touched; the config-class gate (CI `build`) is the coverage.
- **RDD / ADE / SDD:** the schema `version:` is the SDD config head; restored to the one the pinned charly supports.
- **Hard cutover:** one atomic commit.
- **Kernel/plugin boundary law:** `N/A`.
- **Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A` (no Go).
- **CHANGELOG:** `N/A` — post-merge by `tag-on-merge`.

---

*Assisted-by: opencode ollama-cloud deepseek-v4.1-flash (fully tested and validated)*