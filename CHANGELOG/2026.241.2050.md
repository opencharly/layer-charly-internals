# 2026.241.2050 — fix(skill): replace stale check-local refs with the live roster

## Summary

The `/charly-internals:agents` skill's `parallel-bed-testing.md` reference referenced the retired `check-local` bed in three places: the `/verify-status` disqualification list ("`check-local` is host-local"), the partition example, and the worked partition. The live roster has `check-local-vm` (a VM bed running the `kind: local` path inside the disposable eval VM) and the `check-*-local` host-local beds. All three references updated to the live roster.

## How tested

- `python3 -c "import yaml; yaml.safe_load(open('charly.yml'))"` → `YAML OK`.
- `grep -n 'check-local' charly.yml` → only the three updated references remain, all pointing at `check-local-vm` / `check-*-local`.
- Bed names verified against the live roster (`grep '^check-.*:' charly/charly.yml`).

## Rulebook compliance

- R1: doc-vs-reality divergence (stale bed name) — RCA'd and fixed in the owning repo.
- R5: the retired `check-local` identifier swept from the skill content.
- Docs-only change class (skill entity content in `charly.yml`).

## Change classification

- **Change class:** documentation (skill entity content)
- **Verification gate:** documentation reviewed
- **Attribution tier:** documentation reviewed

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*