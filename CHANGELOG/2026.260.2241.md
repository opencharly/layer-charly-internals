# 2026.260.2241 — docs(git-workflow): document the model-free bot-body footer form

## Summary of changes

Mirror the merged A1 amendment (`opencharly/action-review#6`) into the `/charly-internals:git-workflow`
skill's squash-merge attribution section (owned by this repo). The section carried only the
AI-authored form `*Assisted-by: <Harness> <Provider Full Model Name> (<confidence>)*`; it now
also documents the MODEL-FREE `<Harness> <Runtime>` form a fixed CI generator must use (e.g.
`*Assisted-by: GitHub Actions ubuntu-latest (fully tested and validated)*`), and forbids both an
`N/A` provider placeholder and a fabricated AI model name.

**Closes / relates to:** mirrors `opencharly/action-review#6` (merged).

**Breaking change + rollback:** none — one added paragraph; revert restores the prior text.

**Files in the diff** (`git diff --stat origin/main...HEAD`):
```
 charly.yml | 11 +++++++++++
 1 file changed, 11 insertions(+)
```

## Change class

**candy/box config** (skill-entity documentation).

## How R10-tested — exact gate, commands, and pasted output

- **The gate:** `charly box validate` plus the non-runtime standards.
- **Eval beds run:** `N/A — skill-entity documentation`.
- **Fresh rebuild:** `N/A — no binary`.
- **Did the CHANGED code path execute live?** `N/A — no code path`.

**Evidence — put each item in the block whose label names it.**

*1. Fresh-rebuild gate output:* `N/A`.
*2. Each changed code path executing:* `N/A`.
*3. Docs-only evidence:*
```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:skills` — edit the owning repo's `skill:` entity; this repo
  owns `git-workflow`.
- **R1 RCA + zero warnings:** RCA'd to the A1 amendment not being mirrored; validate reports 0/0.
- **R2 no out-of-scope:** same theme; nothing parked.
- **R3 no duplication:** the authoritative clause lives in `action-review:prompt/validator.md`;
  this is the skill mirror.
- **R4 no workaround:** the skill now matches the rulebook the gate enforces.
- **R5 hard cutover + grep:** no identifier removed; the clause cites the authoritative source.
- **R6 git safety:** `git status` checked; no force-push.
- **R7 runtime gate:** `N/A`.
- **R8 artifact invariants:** `N/A`.
- **R9 binary == source:** `N/A`.
- **R10 disposable + coverage:** `N/A — documentation change class`.
- **RDD / ADE / SDD:** RDD — the amendment's premise was verified by the two blocked generator
  PRs. ADE — unchanged. SDD — `N/A`.
- **Hard cutover:** one atomic commit.
- **Kernel/plugin boundary law:** `N/A`.
- **Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A — no Go`.
- **CHANGELOG:** this repo keeps `CHANGELOG/`; tag-on-merge writes the entry from this body.

## Attribution tier

`documentation reviewed` — the whole diff is skill prose; `charly box validate` passes.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*