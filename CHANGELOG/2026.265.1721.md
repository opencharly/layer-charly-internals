# 2026.265.1721 — docs(git-workflow): ONE org ruleset replaces the per-repo branch protection

## Summary

Documentation-only sweep: the `git-workflow` skill now describes the org-wide validator/branch-protection as **ONE organization ruleset**, replacing the retired per-repo branch rulesets and per-repo dispatcher stubs.

The claim-keyed sweep fixes every stale reference in this candy's `skill: git-workflow-skill` entity:

- **ONE organization branch ruleset.** Both the skill **header** and the "No direct push to `main`" **invariant bullet** now state that on GitHub Team the single ruleset carries BOTH the branch rules (`creation`/`deletion`/`non_fast_forward` + strict required `validate / validate`) AND the `workflows` rule ("Require workflows to pass") naming `opencharly/.github/.github/workflows/org-wide-pr-validator-required.yml`, so no repo installs a dispatcher stub.
- **`branch-protection.sh` → `org-ruleset.sh`.** The Cross-References entry and the legacy-API sentence name the current single owner; the legacy branch-protection API removal is now attributed to `org-ruleset.sh`.
- **The required check is `validate / validate`** (the check run), not the workflow name `charly/pr-validator` — corrected where it claimed otherwise.
- **Re-trigger mechanics:** re-dispatch the reusable validator in `opencharly/.github`, not a per-repo `pr-validator.yml`; the no-content-push trigger is the org required workflow's `on: pull_request`.
- **`14 today` → `~409 today`** active `main`-default repos.
- **Dropped the nonexistent `auto-merge` workflow** claim: the validator's own workflow arms native auto-merge (squash) inline.

Generated projection regenerated separately (marketplace + docs) once this source lands and tags.

## How tested

Documentation-only change class; no runtime bed.

```console
$ grep -nE "branch-protection\.sh|org-wide-pr-validator-dispatcher|per-repo branch RULESET|14 today|is the required check" layer-charly-internals/charly.yml
$ echo $?
1
```

(The sweep prints nothing — no stale claim survives. The pattern keys on the actual stale claim; the added prose uses `per-repo stub`, which is the current-state wording, not the retired mechanism.)

## Rulebook compliance

- **R0 skills:** `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1 RCA:** the stale claims are a doc-vs-reality divergence from the org-ruleset cutover; swept claim-keyed.
- **R2 complete:** every variant of the claim fixed in this landing.
- **R3 no duplication:** the skill keeps pointing at the single owner script.
- **R5:** the claim-keyed sweep is executed above.
- **R6 git safety:** branch + PR; no force-push (round-2 fix is an appended commit, never an amend).
- **R7 gate:** the sweep output pasted above.
- **Change class:** documentation-only → `documentation reviewed`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*